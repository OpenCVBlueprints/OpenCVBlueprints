cmake_minimum_required(VERSION 2.8)
project( rotation_invariant_detection )
find_package( OpenCV REQUIRED )
add_executable( rotation_invariant_detection rotation_invariant__object_detection.cpp )
target_link_libraries( rotation_invariant_detection ${OpenCV_LIBS} )
